package au.org.ala.bie.search

/**
 * A enum of index doc types.
 */
enum IndexDocType {
    TAXON,
    LAYER,
    REGION,
    DATARESOURCE,
    DATAPROVIDER,
    COLLECTION,
    INSTITUTION,
    SPECIESLIST,
    HABITAT,
    WORDPRESS,
    COMMON,
    IDENTIFIER,
}
